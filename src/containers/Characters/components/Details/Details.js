import React from 'react';
import PropTypes from 'prop-types';

const Details = ({ details }) => <div>Details</div>;

Details.propTypes = {
  details: PropTypes.shape({}),
};

export default Details;
